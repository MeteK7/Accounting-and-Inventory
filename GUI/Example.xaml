<Window x:Class="GUI.Example"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        Title="Example" Height="450" Width="800">
    <Window.CommandBindings>
        <CommandBinding Command="Save" CanExecute="CommandBindingNew_CanExecute" Executed="Button_Click" />
    </Window.CommandBindings>
    <Window.InputBindings>
        <KeyBinding Key="F5" Command="{Binding RunCommand}" />
    </Window.InputBindings>
    <Grid>
        <Button Command="Save" HorizontalAlignment="Left" Margin="176,39,0,0" VerticalAlignment="Top" Click="Button_Click" Content="SAVE"/>
        <Button HorizontalAlignment="Left" Margin="176,99,0,0" VerticalAlignment="Top" Click="Button_Click" Content="RUN"/>
    </Grid>
</Window>
